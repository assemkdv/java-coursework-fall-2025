# Sum of Digits Calculator
---

## Overview
This program practices writing and calling methods in Java, specifically focusing on numeric processing and method return values. You will create a method named `digitSum` that accepts an integer as a parameter and returns the **sum of all its digits**.

The method must work correctly for:
- **Positive integers**  
- **Negative integers** (treat as positive)  
- **Zero**  

For example:  
- `digitSum(29107) → 19`  
- `digitSum(-456) → 15`  
- `digitSum(0) → 0`

The main program should prompt the user for an integer, call the `digitSum` method, and print the result.

---

## Objective
- Practice defining and calling **static methods**.  
- Use loops and arithmetic to extract digits from integers.  
- Apply **Math.abs()** to correctly handle negative input.  
- Strengthen problem-solving skills involving modular arithmetic.

---

## Sample Output

```text

Enter an integer: 
4562
The digit sum is: 17

Enter an integer: 
-789
The digit sum is: 24

Enter an integer: 
0
The digit sum is: 0

